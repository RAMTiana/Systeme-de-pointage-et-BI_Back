# ==============================================================
# Dépendances applicatives — Backend SRB Haute Matsiatra
# ==============================================================
# NOTE : ce fichier remplace un ancien requirements.txt corrompu qui
# contenait un pip freeze du système Debian de la machine de développement
# (Brlapi, cupshelpers, python-apt, reportbug, vboxapi...) au lieu des
# dépendances réelles du projet. Résultat : `pip install -r requirements.txt`
# échouait systématiquement (Brlapi n'existe pas sur PyPI), et même en cas
# de succès partiel, fastapi/sqlalchemy/uvicorn n'auraient jamais été
# installés — l'application ne pouvait probablement pas démarrer depuis un
# environnement neuf avec ce fichier.

# --- Framework web ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
starlette==0.41.3
python-multipart==0.0.20

# --- Validation / configuration ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Base de données ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# --- Sécurité / Auth ---
argon2-cffi==23.1.0
bcrypt==4.2.1
python-jose[cryptography]==3.3.0
cryptography==44.0.2
google-auth==2.37.0
webauthn==2.5.2

# --- Rate limiting / cache / verrouillage de compte ---
redis==5.2.1
slowapi==0.1.9

# --- Génération de rapports (Excel / PDF) ---
openpyxl==3.1.5
reportlab==4.2.5

# --- HTTP sortant (Google OIDC, webhook SMS) ---
requests==2.32.3

# --- Divers ---
python-dateutil==2.9.0.post0
